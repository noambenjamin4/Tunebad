node_modules
.next
bin/
out
dist
.env
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

tsconfig.tsbuildinfo
.vercel
.env*
.venv*

# Home Bridge secrets
# Any variant — a `.bak` copy of this file once sat here untracked with a
# live API_KEY in it, one `git add -A` from being published.
scripts/tunebad-bridge.env*
logs/
AUTONOMOUS-QUEUE.md

# Local DB backups (4MB+ dumps, never commit)
backups/

# graphify build artifacts (regenerated on every run; includes live pid/log)
graphify-out/
